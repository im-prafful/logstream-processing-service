# ============================================================
# LOCAL GPU REQUIREMENTS — DO NOT DEPLOY TO AWS
# ============================================================
# Run ONCE on your local machine to enable CUDA for training:
#   pip install -r requirements-local-gpu.txt
#
# This overrides the CPU-only torch from requirements.txt
# with the CUDA 12.1 build for your NVIDIA RTX 3050.
# AWS ECS uses requirements.txt (CPU-only) — this file is ignored there.
# ============================================================

torch==2.1.0 --index-url https://download.pytorch.org/whl/cu121
sentence-transformers
transformers<4.41   # transformers>=4.41 uses register_pytree_node which requires torch>=2.2
numpy<2             # torch 2.1.0 was compiled against NumPy 1.x — NumPy 2.x breaks it
