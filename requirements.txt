# --- Data Handling & Database Connection ---
pandas
sqlalchemy
psycopg2-binary

# --- Core ML: Streaming & Embeddings ---
river
sentence-transformers==3.3.1
transformers==4.46.3
# NOTE: torch is installed separately in Dockerfile (CPU-only version)
numpy<2   # Must stay on 1.x for torch/scipy ABI compatibility

# --- Vector Search & Math (CRITICAL FOR NEW ENGINE) ---
scipy>=1.10.0   # Required for Minkowski distance
scikit-learn    # Required dependency for many ML vector ops

# --- Model Persistence ---
joblib

# --- Configuration ---
python-dotenv

# --- API Server (For Future Use) ---
fastapi
uvicorn[standard]